// All test modules are now included from source via #[cfg(test)] #[path].
// The assistant subdirectory is also sourced from the source modules.
// Keep this file for the mod entry point so the test framework discovers it,
// but the actual test modules are included from source files.
